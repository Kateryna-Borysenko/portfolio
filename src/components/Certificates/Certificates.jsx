import Carousel from '../common/Carousel/Carousel';
import { certificates } from '../../data/certificates';

const Certificates = () => {
  return <Carousel items={certificates} />;
};

export default Certificates;

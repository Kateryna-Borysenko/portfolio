import { certificates } from '../../data/certificates';
import s from './Certificates.module.css';

const Certificates = () => {
  return <div className={s.container}>Certificates</div>;
};

export default Certificates;
